---
layout: base.njk
---

{% for module in modules %}
  {% set knownTypes = ["grid", "hero", "project-hero", "single-column", "spacer"] %}
  {% if module.type in knownTypes %}
    {% include "modules/" + module.type + ".njk" %}
  {% endif %}
{% endfor %}
